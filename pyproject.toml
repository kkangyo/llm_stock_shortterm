[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-news-bot"
version = "0.1.0"
description = "뉴스 기반 호재 탐지 파이프라인 (Ollama 1차 필터 + Claude 2차 분석)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
